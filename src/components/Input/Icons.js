import styled from "styled-components";

import { User } from "@styled-icons/boxicons-solid";
import { Lock } from "@styled-icons/boxicons-solid";
import { Droplet } from "@styled-icons/boxicons-solid";

export const UsernameIcon = styled(User)`
  width: 25px;
`;

export const PasswordIcon = styled(Lock)`
  width: 25px;
`;

export const DropletIcon = styled(Droplet)`
  width: 25px;
`;
